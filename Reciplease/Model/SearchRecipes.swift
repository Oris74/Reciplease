//
//  Recipe.swift
//  Reciplease
//
//  Created by Laurent Debeaujon on 04/11/2020.
//

/*import Foundation

class RecipeSelection {
    static let shared = RecipeSelection()

    private(set) var recipes = [RecipleaseStruct]()
    //private(set) var favorites: [String]

    private let
        fridgeContent = FridgeService.shared.fridge.stringOfIngredients

    private var position: Int = 0
    private var count: Int = 0

    func searchRecipes(callback: @escaping (Utilities.ManageError?,[RecipleaseStruct]?) -> Void ) {

        Services.shared.getRecipes(ingredients: fridgeContent, completion: { (error, recipes) in

            callback(error, recipes)
        })

    }
/*
     func getFavorites() {
     APIServices.shared.getAPIData(id: favorites ) { (error, apiData) in
     if (error == nil) {
     guard let data = apiData, let recipes = data.recipes else {
     self.error = .incorrectDataStruct
     return
     }
     self.recipesRange = recipes
     self.position = data.toQty
     self.count = data.totalCount
     } else {
     self.error = error
     }
     }
     }*/
}*/
